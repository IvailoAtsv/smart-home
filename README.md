# Умен дом

Прост план за монтаж: Home Assistant + Shelly + глас на български.

```bash
npm install
npm run dev
```

Отвори [http://localhost:3000](http://localhost:3000). Deploy: Vercel + този repo.
